<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.coffwaiter">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.COFFwaiter">
        <activity android:name=".ui.ready_order.ReadyOrderPage"/>
        <activity android:name=".ui.ready_order.ThankYouPage" />
        <activity android:name=".ui.restaurant.RestaurantActivity" />
        <activity android:name=".ui.qr_scan.QrScanActivity" />
        <activity android:name=".ui.start.StartActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>